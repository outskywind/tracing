<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dafy.skye.zipkin.extend.mapper.RuleMapper" >

    <insert id="add">
        INSERT INTO rule ( `name`, type, service, interface, dimension, `condition`, threshold )
        VALUES
        <foreach collection="list" separator="," item="rule">
        ( #{rule.type}, #{rule.type}, #{rule.service}, #{rule.spanName}, #{rule.dimension}, #{rule.condition}, #{rule.threshold} )
        </foreach>
    </insert>

    <select id="list" resultType="com.dafy.skye.zipkin.extend.dto.Rule">
        SELECT `id`,
        `name`,
        `type`,
        `service`,
        `interface`,
        `dimension`,
        `condition`,
        `threshold`,
        `is_deleted`
        FROM `rule`
        where service in <foreach collection="favServices" open="(" close=")" item="service" separator=",">
         #{service}
    </foreach>
        UNION ALL
        SELECT `id`,
        `name`,
        `type`,
        `service`,
        `interface`,
        `dimension`,
        `condition`,
        `threshold`,
        `is_deleted`
        FROM `rule`
        where service =''
    </select>

    <update id="modify">
        UPDATE rule
        <set>
            condition = #{condition},
            threshold = #{threshold}
        </set>
        where id=#{id}
    </update>

    <update id="delete">
        UPDATE
        rule
        SET is_deleted=1
        where id IN
        <foreach collection="list" item="rule" open="(" separator="," close=")">
        #{rule.id}
        </foreach>
    </update>

</mapper>